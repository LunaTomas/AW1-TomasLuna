index siempre en minuscula
cajas --> html
arbol -->
raiz --> root
control + "}" autocomentar
viewport --> porcion del navegador que muestra la pagina
h1 solo deberia haber 1 por documento

usar html:5 al iniciar index